current_dir=$(pwd)

if [[ $(basename $current_dir) = "source" ]]; then
  project_root="$current_dir/.."
else
  project_root="$current_dir"
fi

hook_path="$project_root/rvm_hooks/after_use"

if [[ -s $hook_path ]]; then
  . $hook_path
fi
